# Welcome to your knowledge base

This folder is served by `mcp_server.py`. Every `.md` file under it becomes searchable
through the `search_kb` tool and readable through `get_doc`.

Drop your own notes in here, restart nothing — the folder is scanned per request.
